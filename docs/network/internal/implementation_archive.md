# Roy-06 24/7 稳定性硬化方案

## 1. 核心目标

将 roy-06 (Windows) 打造为“全天候、高可用、低功耗”的首席跳板机。

## 2. 今日已完成 (2026-03-02)

- [x] **服务固化**：SSH 和 Tailscale 设为自动启动。
- [x] **极速路由**：写入 10.0.0.0/8 静态持久路由 (1ms)。
- [x] **极致精减**：禁用打印、索引等后台冗余服务。
- [x] **全网接入**：
  - Roy-01 (本机): 经 06 跳转 GPU (1ms)。
  - Roy-02 (家): 经 06 跳转 GPU (< 20ms)。
- [x] **隐私操作**：已手动执行远程指令关闭物理屏幕。

---

## 3. 次日待办：极致静默与安全性 [等待计划批准] (Tomorrow Focus)

> [!IMPORTANT]
> **明天我将主动向老板汇报以下计划，请在批准后再执行：**

### A. 纠正自动登录 (Auto-Logon)

- **目标**：解决目前凭据报错导致屏幕卡死的问题。
- **操作**：使用正确密码更新注册表加密字段。

### B. 登录即锁屏 (Auto-Lock on Boot)

- **方案**：在启动文件夹部署自动化脚本，实现开机后立即锁定 Workstation。
- **效果**：物理屏幕处于锁定状态，无法被他人直接操作。

### C. 启动级息屏 (Auto-Screen-Off)

- **方案**：将息屏指令固化为启动项。
- **效果**：即使由于系统重启触发了屏幕点亮，也会在登录后的 1 秒内强制切回节能黑屏模式。

**老板请先行休息，明天见！**
