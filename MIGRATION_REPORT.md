# ResearchBot 迁移报告 (已完成)

## 1. "零代码" 科研助手重构 (Zero-Code Refactor)
-   **状态**: **技能完备 (Skills Complete)**
-   **能力**: `research-assistant` 支持远程代码生成、LaTeX 编译、数据标准化。

## 2. 架构模式 (Architecture)
OpenClaw 现已配置为 **"远程优先，本地镜像" (Remote-First, Local-Mirror)** 架构。

### B. 资产分布 (Asset Distribution)
| 资产类型 | 存储位置 | 同步策略 |
| :--- | :--- | :--- |
| **代码/论文** | 远程 + 本地 | ✅ **双向同步 (Git)** |
| **模型/数据** | **远程独享** | 🚫 **不拉回** (符号链接共享) |

## 3. 工作流手册 (Workflow Manual)

### 场景 A: Agent 自动科研 (推荐)
-   **操作者**: 我 (Agent)
-   **流程**: 本地生成代码 -> `deploy_remote.py` 推送 -> 远程执行。

### 场景 B: 人类直连开发 (VS Code Remote)
-   **操作者**: 您 (User)
-   **连接**: `ssh research-bot` (已配置别名)
-   **原理**: 连接后，VS Code **直接运行在远程服务器上**。
    -   您看到的那个终端 = **远程终端**。
    -   您看到的 Git 面板 = **远程 Git**。
    -   您做的任何修改 = **直接修改远程文件**。

-   **Git 身份与权限**:
    -   **身份**: 已自动同步您的身份 (`royliu761201`)，Commit 记录与本地一致。
    -   **权限**: 已开启 **SSH Agent Forwarding**。远程 Git 会直接借用您本地电脑的 SSH Key 来拉取/推送 GitHub 私有仓库，无需在服务器上存密钥。

## 4. 远程环境详情
| Item | Details |
| :--- | :--- |
| **Host Alias** | `research-bot` |
| **Direct IP** | `10.190.30.220` |
| **Workspace** | `/root/research_bot` |

### 数据组织标准 (Symlink Strategy)
1.  **Model Zoo** -> `/root/research_bot/models/` (共享)
2.  **Data Lake** -> `/root/research_bot/data/` (共享)
3.  **Archive** -> `/root/research_bot/archive/` (归档)

## 下一步
1.  **连接**: `ssh research-bot` 测试连接。
2.  **开发**: 使用 VS Code Remote 打开 `/root/research_bot`。
3.  **同步**: 养成 "修改完记得 Commit" 的习惯。
